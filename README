DEPENDENCIES

- Python-2.7
- libyaml-0-2
- libyaml-0-2-dbg [optional]
- libyaml-dev

BUILD with Automake

mkdir build
cd build
../configure --prefix=<prefix> [CFLAGS=... LDFLAGS=...]
make
make install


RPM BUILD

mkdir build
cd build
../configure --prefix=<prefix> [CFLAGS=... LDFLAGS=...]
make rpm7
# After finished, the RPMs will be in rpm7/RPMS/<arch>/ directory.
